<Page x:Class="Ets2Pilot.Gui.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:gui="clr-namespace:Ets2Pilot.Gui"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance gui:MainViewModel, IsDesignTimeCreatable=False}"
      Title="{DynamicResource Text.Nav.Settings}">
    <Page.Resources>
        <Style x:Key="SettingRow" TargetType="Border" BasedOn="{StaticResource CardRow}">
            <Setter Property="MinHeight" Value="68" />
            <Setter Property="Padding" Value="16,12" />
        </Style>
        <Style x:Key="SettingLabel" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,0,24,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <StackPanel Margin="24,16,24,24">
        <TextBlock Style="{StaticResource SectionHeader}" Margin="4,0,4,8" Text="{DynamicResource Text.Settings.ModelHeader}" />
        <ui:Card Padding="0">
            <StackPanel>
                <Border Style="{StaticResource SettingRow}" BorderThickness="0">
                    <DockPanel>
                        <ui:TextBox DockPanel.Dock="Right" Width="360" VerticalAlignment="Center"
                                    FontFamily="{StaticResource MonoFont}" FontSize="13"
                                    ClearButtonEnabled="False"
                                    PlaceholderText="{DynamicResource Text.Settings.RepoPlaceholder}"
                                    Text="{Binding ModelRepo}" />
                        <StackPanel Style="{StaticResource SettingLabel}">
                            <TextBlock Text="{DynamicResource Text.Settings.Repo}" />
                            <TextBlock Margin="0,2,0,0" Style="{StaticResource CaptionText}" Text="{DynamicResource Text.Settings.RepoHint}" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Border Style="{StaticResource SettingRow}">
                    <DockPanel>
                        <ui:TextBox DockPanel.Dock="Right" Width="360" VerticalAlignment="Center"
                                    FontFamily="{StaticResource MonoFont}" FontSize="13"
                                    ClearButtonEnabled="False"
                                    PlaceholderText="{DynamicResource Text.Settings.RevisionPlaceholder}"
                                    Text="{Binding ModelRevision}" />
                        <StackPanel Style="{StaticResource SettingLabel}">
                            <TextBlock Text="{DynamicResource Text.Settings.Revision}" />
                            <TextBlock Margin="0,2,0,0" Style="{StaticResource CaptionText}" Text="{DynamicResource Text.Settings.RevisionHint}" />
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Border Style="{StaticResource SettingRow}">
                    <DockPanel>
                        <ComboBox DockPanel.Dock="Right" Width="360" VerticalAlignment="Center"
                                  ItemsSource="{Binding Mirrors}"
                                  DisplayMemberPath="Label"
                                  SelectedItem="{Binding SelectedMirror}" />
                        <StackPanel Style="{StaticResource SettingLabel}">
                            <TextBlock Text="{DynamicResource Text.Settings.Mirror}" />
                            <TextBlock Margin="0,2,0,0" Style="{StaticResource CaptionText}" Text="{DynamicResource Text.Settings.MirrorHint}" />
                        </StackPanel>
                    </DockPanel>
                </Border>
            </StackPanel>
        </ui:Card>

        <TextBlock Style="{StaticResource SectionHeader}" Margin="4,20,4,8" Text="{DynamicResource Text.Settings.StorageHeader}" />
        <ui:Card Padding="0">
            <Border Style="{StaticResource SettingRow}" BorderThickness="0">
                <DockPanel>
                    <ui:Button DockPanel.Dock="Right" VerticalAlignment="Center" Content="{DynamicResource Text.Settings.Open}"
                               Icon="{ui:SymbolIcon Folder24}" Command="{Binding OpenDataDirCommand}" />
                    <StackPanel Style="{StaticResource SettingLabel}">
                        <TextBlock Text="{DynamicResource Text.Settings.DataDir}" />
                        <TextBlock Margin="0,2,0,0" Style="{StaticResource CaptionText}">
                            <Run FontFamily="{StaticResource MonoFont}" Text="{Binding DataDir, Mode=OneWay}" /><Run Text=" · " /><Run Text="{DynamicResource Text.Settings.DataDirHint}" />
                        </TextBlock>
                    </StackPanel>
                </DockPanel>
            </Border>
        </ui:Card>
    </StackPanel>
</Page>
