<ui:FluentWindow x:Class="Etssd.Gui.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:Etssd.Gui"
                 xmlns:pages="clr-namespace:Etssd.Gui.Pages"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance local:MainViewModel, IsDesignTimeCreatable=False}"
                 Title="etssd" Width="800" Height="600" MinWidth="640" MinHeight="480"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <ui:FluentWindow.Resources>
        <!-- Tag 绑定组件是否运行 -->
        <Style x:Key="ServiceDot" TargetType="Ellipse">
            <Setter Property="Width" Value="8" />
            <Setter Property="Height" Value="8" />
            <Setter Property="Fill" Value="Transparent" />
            <Setter Property="Stroke" Value="{DynamicResource TextFillColorTertiaryBrush}" />
            <Setter Property="StrokeThickness" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Fill" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                    <Setter Property="Stroke" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="etssd">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="VehicleTruckProfile24" Filled="True" Foreground="{DynamicResource AccentFillColorDefaultBrush}" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <ui:NavigationView x:Name="RootNavigation" Grid.Row="1"
                           PaneDisplayMode="Top"
                           IsBackButtonVisible="Collapsed"
                           IsPaneToggleVisible="False"
                           FrameMargin="0">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="概览" TargetPageType="{x:Type pages:OverviewPage}" />
                <ui:NavigationViewItem Content="日志" TargetPageType="{x:Type pages:LogsPage}" />
                <ui:NavigationViewItem Content="设置" TargetPageType="{x:Type pages:SettingsPage}" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <Border Grid.Row="2" Height="28" Padding="24,0" BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                        TextElement.FontSize="12"
                        TextElement.Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </StackPanel.Resources>
                <Ellipse Style="{StaticResource ServiceDot}" Tag="{Binding IsBridgeRunning}" />
                <TextBlock Margin="6,0,0,0" Text="bridge" />
                <TextBlock Margin="4,0,20,0" FontFamily="{StaticResource MonoFont}" Text="{Binding BridgePort, StringFormat=:{0}}" />
                <Ellipse Style="{StaticResource ServiceDot}" Tag="{Binding IsControlRunning}" />
                <TextBlock Margin="6,0,0,0" Text="control" />
                <TextBlock Margin="4,0,20,0" FontFamily="{StaticResource MonoFont}" Text="{Binding ControlPort, StringFormat=:{0}}" />
                <Ellipse Style="{StaticResource ServiceDot}" Tag="{Binding IsInferRunning}" />
                <TextBlock Margin="6,0,0,0" Text="模型" />
                <TextBlock Margin="4,0,20,0" FontFamily="{StaticResource MonoFont}" Text="{Binding ModelPort, StringFormat=:{0}}" />
                <Rectangle Width="1" Height="14" Margin="0,0,20,0" Fill="{DynamicResource CardStrokeColorDefaultBrush}" />
                <TextBlock Text="已注册回调：" />
                <TextBlock FontFamily="{StaticResource MonoFont}" Text="{Binding Callbacks}" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
