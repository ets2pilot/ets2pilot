<Page x:Class="Etssd.Gui.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:gui="clr-namespace:Etssd.Gui"
      xmlns:logging="clr-namespace:Microsoft.Extensions.Logging;assembly=Microsoft.Extensions.Logging.Abstractions"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance gui:MainViewModel, IsDesignTimeCreatable=False}"
      Title="日志">
    <Grid Margin="24,16,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <ui:Button DockPanel.Dock="Right" Margin="12,0,0,0" Content="打开日志目录"
                       Icon="{ui:SymbolIcon Folder24}" Command="{Binding OpenLogDirCommand}" />
            <ui:Button DockPanel.Dock="Right" Content="打开日志文件"
                       Icon="{ui:SymbolIcon Document24}" Command="{Binding OpenLogFileCommand}" />
            <ui:ToggleSwitch x:Name="AutoScroll" Content="自动滚动" IsChecked="True" HorizontalAlignment="Left" />
        </DockPanel>

        <ui:Card Grid.Row="1" Padding="0,8" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
            <ListBox x:Name="LogList" ItemsSource="{Binding Logs}"
                     Background="Transparent" BorderThickness="0"
                     FontFamily="{StaticResource MonoFont}" FontSize="12"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="Padding" Value="16,2" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type gui:LogEntry}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="64" />
                                <ColumnDefinition Width="52" />
                                <ColumnDefinition Width="176" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                       Text="{Binding Time, StringFormat=HH:mm:ss}" />
                            <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding LevelText}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Warning}">
                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Error}">
                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Critical}">
                                                <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Grid.Column="2" Margin="0,0,12,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis" Text="{Binding Category}" ToolTip="{Binding Category}" />
                            <TextBlock Grid.Column="3" TextTrimming="CharacterEllipsis" Text="{Binding Message}" ToolTip="{Binding Message}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ui:Card>

        <DockPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock DockPanel.Dock="Right" Style="{StaticResource CaptionText}"
                       Text="{Binding MaxLogCount, StringFormat=窗口内保留最近 {0} 行}" />
            <TextBlock Style="{StaticResource CaptionText}">
                <Run Text="文件：" /><Run FontFamily="{StaticResource MonoFont}" Text="{Binding LogFile, Mode=OneWay}" /><Run Text=" · 反馈问题时直接发送此文件" />
            </TextBlock>
        </DockPanel>
    </Grid>
</Page>
