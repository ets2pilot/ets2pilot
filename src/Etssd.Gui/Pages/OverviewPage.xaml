<Page x:Class="Etssd.Gui.Pages.OverviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:gui="clr-namespace:Etssd.Gui"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance gui:MainViewModel, IsDesignTimeCreatable=False}"
      Title="概览">
    <Page.Resources>
        <Style x:Key="Chip" TargetType="Border">
            <Setter Property="Height" Value="22" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="CornerRadius" Value="11" />
            <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
            <Setter Property="TextElement.FontSize" Value="12" />
            <Setter Property="TextElement.FontWeight" Value="SemiBold" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        </Style>
    </Page.Resources>

    <StackPanel Margin="24,16,24,24">
        <ui:Card Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Style="{StaticResource StatusIcon}" Tag="{Binding OverviewStatus}" VerticalAlignment="Center" />
                        <TextBlock Margin="10,0,0,0" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" Text="{Binding OverviewTitle}" />
                        <StackPanel Orientation="Horizontal" Margin="4,0,0,0" VerticalAlignment="Center"
                                    Visibility="{Binding IsInferRunning, Converter={StaticResource BoolToVisibility}}">
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource Chip}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding InferFreq}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock VerticalAlignment="Center" Text="{Binding InferFreq}" />
                            </Border>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource Chip}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsControlEnabled}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SystemFillColorSuccessBackgroundBrush}" />
                                                <Setter Property="TextElement.Foreground" Value="{DynamicResource SystemFillColorSuccessBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="控制 · 未启用" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsControlEnabled}" Value="True">
                                                    <Setter Property="Text" Value="控制 · 已启用" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Margin="26,4,0,0" Style="{StaticResource SecondaryText}" Text="{Binding OverviewSubtitle}" />
                </StackPanel>
                <ui:Button Grid.Column="1" Height="32" Padding="20,0" VerticalAlignment="Center">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                            <Setter Property="Appearance" Value="Primary" />
                            <Setter Property="Content" Value="运行模型" />
                            <Setter Property="Icon" Value="{ui:SymbolIcon Play24, Filled=True}" />
                            <Setter Property="Command" Value="{Binding InferCommand}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInferRunning}" Value="True">
                                    <Setter Property="Appearance" Value="Secondary" />
                                    <Setter Property="Content" Value="停止" />
                                    <Setter Property="Icon" Value="{ui:SymbolIcon Stop24, Filled=True}" />
                                    <Setter Property="Command" Value="{Binding InferCancelCommand}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                </ui:Button>
            </Grid>
        </ui:Card>

        <DockPanel Margin="0,20,0,8">
            <ui:Button DockPanel.Dock="Right" Content="重新检查" Icon="{ui:SymbolIcon ArrowClockwise24}"
                       Command="{Binding RunChecksCommand}" />
            <TextBlock Style="{StaticResource SectionHeader}" Text="准备情况" />
        </DockPanel>

        <ui:Card Padding="0">
            <StackPanel>
                <Border Style="{StaticResource CardRow}" BorderThickness="0" MinHeight="48" Padding="16,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="88" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:SymbolIcon Grid.Column="0" Style="{StaticResource StatusIcon}" Tag="{Binding ModelStatus}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Margin="12,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center" Text="模型" />
                        <StackPanel Grid.Column="2" Margin="12,0" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource SecondaryText}" Text="{Binding ModelMessage}" ToolTip="{Binding ModelMessage}" />
                            <ProgressBar Margin="0,6,0,0"
                                         Value="{Binding ModelProgress, Mode=OneWay}"
                                         IsIndeterminate="{Binding IsModelProgressIndeterminate}"
                                         Visibility="{Binding IsModelBusy, Converter={StaticResource BoolToVisibility}}" />
                        </StackPanel>
                        <ui:Button Grid.Column="3" VerticalAlignment="Center">
                            <ui:Button.Style>
                                <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                    <Setter Property="Content" Value="检查更新" />
                                    <Setter Property="Command" Value="{Binding CheckModelCommand}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsModelBusy}" Value="True">
                                            <Setter Property="Content" Value="取消" />
                                            <Setter Property="Command" Value="{Binding CheckModelCancelCommand}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:Button.Style>
                        </ui:Button>
                    </Grid>
                </Border>

                <ItemsControl ItemsSource="{Binding Checks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type gui:CheckRow}">
                            <Border Style="{StaticResource CardRow}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="88" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ui:SymbolIcon Grid.Column="0" Style="{StaticResource StatusIcon}" Tag="{Binding Status}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Margin="12,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center" Text="{Binding Name}" />
                                    <TextBlock Grid.Column="2" Margin="12,0" Style="{StaticResource SecondaryText}" Text="{Binding Message}" ToolTip="{Binding Message}" />
                                    <ui:HyperlinkButton Grid.Column="3" VerticalAlignment="Center" Padding="4"
                                                        Content="{Binding LinkText}" NavigateUri="{Binding Link}"
                                                        Icon="{ui:SymbolIcon Open16}">
                                        <ui:HyperlinkButton.Style>
                                            <Style TargetType="ui:HyperlinkButton" BasedOn="{StaticResource {x:Type ui:HyperlinkButton}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Link}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:HyperlinkButton.Style>
                                    </ui:HyperlinkButton>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ui:Card>
    </StackPanel>
</Page>
